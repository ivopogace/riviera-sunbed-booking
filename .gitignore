# IDE
.idea/
*.iml
.vscode/

# OS
Thumbs.db
.DS_Store

# Build output (added as the stack is scaffolded)
/platform/build/
/platform/.gradle/
/frontend/dist/
node_modules/

# JVM crash dumps
hs_err_pid*.log
replay_pid*.log

# Local env / secrets — never commit Stripe keys, DB creds, etc.
*.env
.env.*
!.env.example

# Claude Code personal/local settings (not team-wide)
.claude/settings.local.json
