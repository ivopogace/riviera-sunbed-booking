# IDE
.idea/
*.iml
.vscode/

# OS
Thumbs.db
.DS_Store

# Build output (added as the stack is scaffolded)
/backend/build/
/backend/.gradle/
/frontend/dist/
node_modules/

# JVM crash dumps
hs_err_pid*.log
replay_pid*.log

# Local env / secrets — never commit Stripe keys, DB creds, etc.
*.env
.env.*
!.env.example
