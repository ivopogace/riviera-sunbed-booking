{
  "mcpServers": {
    "angular-cli": {
      "command": "node",
      "args": [
        "${CLAUDE_PROJECT_DIR:-.}/frontend/node_modules/@angular/cli/bin/ng.js",
        "mcp"
      ]
    },
    "render": {
      "type": "http",
      "url": "https://mcp.render.com/mcp",
      "headers": {
        "Authorization": "Bearer ${CLAUDE_RENDER_KEY}"
      }
    },
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--headless",
        "--no-sandbox",
        "--isolated",
        "--executable-path",
        "/opt/pw-browsers/chromium"
      ]
    },
    "idea": {
      "url": "http://127.0.0.1:64360/stream",
      "type": "http"
    }
  }
}