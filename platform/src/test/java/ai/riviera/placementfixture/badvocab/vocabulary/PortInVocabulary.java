package ai.riviera.placementfixture.badvocab.vocabulary;

/** A plain (non-sealed) interface — a port — hiding in a {@code vocabulary} surface — must be rejected. */
public interface PortInVocabulary {

	void call();
}
