/**
 * Published <strong>vocabulary</strong> of the {@code audit} module (invariant #11) — the value
 * type {@link ai.riviera.platform.audit.vocabulary.AdminAuditEntry} its one port answers in. Value
 * types only, never ports. Not granted to the composition root: the edge's fence only appends, and
 * it passes primitives.
 */
@org.springframework.modulith.NamedInterface("vocabulary")
package ai.riviera.platform.audit.vocabulary;
