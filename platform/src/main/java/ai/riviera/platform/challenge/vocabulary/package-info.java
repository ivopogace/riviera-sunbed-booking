/**
 * Published <strong>vocabulary</strong> of the {@code challenge} module (invariant #11) — the closed
 * answer set {@link ai.riviera.platform.challenge.vocabulary.ChallengeVerdict} its one port speaks
 * in. Value types only, never ports. Granted as {@code challenge::vocabulary} to the composition
 * root, which maps each verdict to a problem body at the edge.
 */
@org.springframework.modulith.NamedInterface("vocabulary")
package ai.riviera.platform.challenge.vocabulary;
