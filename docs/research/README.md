# Research notes

Findings from the `research` skill land here — one `docs/research/<slug>.md` per
question, committed on the branch of the ticket that asked it. A note is a **primary
source, not a decision**: when the decision lands, the durable form is an ADR
(`docs/adr/`) or the plan doc, and the note stays as the citation behind it.
